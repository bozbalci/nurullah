#!/usr/bin/env python3

from setuptools import setup
from nurullah import version

setup(
    name='Nurullah',
    version=version,
    description='Terminal music generator',
    packages=['nurullah'],
)
